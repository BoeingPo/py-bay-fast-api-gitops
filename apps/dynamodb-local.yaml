apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dynamodb-local
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/BoeingPo/py-bay-fast-api-gitops
    targetRevision: main
    path: dynamodb-local
  destination:
    server: https://kubernetes.default.svc
    namespace: py-bay-fast-api
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
